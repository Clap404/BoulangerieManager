<div id="container">
	<h1>Bienvenue à la boulanger</h1>

	<div id="body">
		<p>Ici, on mange des baguettes et on fait du lisp comme ça</p>
        <code><pre>

            (mange
              (flatten "gâteau" "pain"
                (map
                  (lambda (x) (string-append "eclair au " x))
                  ("chocolat" "café" "vanille"))))
        </code></pre>

		<p>Et c'est super cool non ? C'est mieu que ça non ?</p>
        <code><pre>
            def modulo (x) : 
                while x > 90 :
                    x -= 90
                while x < 0 :
                    x += 90
                return x
        </code><pre>
    </div>
</div>
